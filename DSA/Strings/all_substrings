#All substrings. For a given input string, write a function to print all the possible substrings

def printSubstrings(string) :

    #Your code goes here.
    for i in range(len(string)):
        for j in range(i,len(string)):
            for k in range(i, (j+1)):
                print(string[k], end="")
            print()
            
          
#main
string = input();
printSubstrings(string)
