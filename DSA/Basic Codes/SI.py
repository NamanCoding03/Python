principal = int(input("Enter the principal interest"))
rate = float(input("Enter the rate of interest"))
time = int(input("Enter the time for which SI needs to be calculated"))
si = principal*rate*time/100

print(si)
